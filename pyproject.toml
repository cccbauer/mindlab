[project]
name = "mindlab"
version = "0.1.0"
description = "Umbrella workspace for mindlab apps (monitor, mindwear, ...)"
requires-python = "==3.12.*"
dependencies = []

[tool.uv.workspace]
members = ["monitor", "mindwear"]

[tool.uv]
package = false
